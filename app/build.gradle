apply plugin: 'com.android.application'

android {
    signingConfigs {
        CarmielKey {
            keyAlias 'CarmielKey'
            keyPassword 'holy3nity'
            storeFile file('C:/Users/AmielRev/Documents/android_2.jks')
            storePassword 'holy3nity'
        }
    }
    compileSdkVersion 19
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "ulanmo.main"
        minSdkVersion 11
        targetSdkVersion 18
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            signingConfig signingConfigs.CarmielKey
        }
    }
}

dependencies {
    compile 'com.android.support:support-v4:19.1.0'
    compile 'com.google.android.gms:play-services:+'
}
